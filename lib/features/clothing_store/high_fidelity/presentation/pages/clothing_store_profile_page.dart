import 'package:flutter/material.dart';

class ClothingStoreProfilePage extends StatelessWidget {
  const ClothingStoreProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Profile Page'),
    );
  }
}
