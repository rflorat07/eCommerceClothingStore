name: ecommerce_ds
description: Design System for eCommerce Flutter applications.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.7
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2
  flutter_svg: ^2.2.4
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  widgetbook: ^3.22.0
